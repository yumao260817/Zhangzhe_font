charset: level1

qa:
  ocr_pass: 0.95
  structure_pass: 0.85
  style_pass: 0.80

rework:
  max_attempts: 3
  fallback_order: [dl, assemble]

generate:
  grid_hw: 256
  anchors: 3

server:
  host: 0.0.0.0
  port: 8000
  admin_token: admin123
  # 邮箱注册用户中，这些邮箱的账号自动获得管理员(审核)权限
  # 本地测试用，正式发布前请填写真实邮箱（提交到 git 前建议删除）

export:
  fmt: ttf
